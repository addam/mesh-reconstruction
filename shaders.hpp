// Generated from shader sources automatically by pack_shaders.awk
const char* vertexShaderSources[] = {"\n#version 330 core\n\n// Input vertex data, different for all executions of this shader.\nlayout(location = 0) in vec3 vertexPosition_modelspace;\nlayout(location = 1) in vec2 vertexUV;\n\n// Output data ; will be interpolated for each fragment.\nout vec3 pos;\n\n// Values that stay constant for the whole mesh.\nuniform mat4 MVP;\n\nvoid main(){\n	gl_Position =  MVP * vec4(vertexPosition_modelspace,1);\n	pos = vertexPosition_modelspace;\n}\n\n"};
const char* fragmentShaderSources[] = {"\n#version 330 core\n\nin vec3 pos;\nout vec3 color;\n\nuniform sampler2D myTextureSampler;\nuniform mat4 sideMVP;\n\nvoid main(){\n	vec4 modelCoord = vec4(pos, 1);\n	vec4 screenCoord = sideMVP * modelCoord;\n	color = texture(myTextureSampler, screenCoord.xy / screenCoord.w).rgb;\n}\n"};
